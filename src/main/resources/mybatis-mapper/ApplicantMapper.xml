<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.maximum.koreaartschool.repository.ApplicantMapper">

<select id="selectApplicantByOption" resultType="com.maximum.koreaartschool.repository.ApplicantMapper">
        SELECT sa.APL_NM, sa.GNDR_CD, sa.APL_BRDT, sa.DEPT_CD, sa.RCRT_CD, sa.EVL_STG_CD,
        ae.IS_EVALUATED, ae.SCORE
        FROM STAGE_APPLICANT sa
        JOIN APPLICANT_EVALUATE ae ON sa.EVL_STG_NO = ae.EVL_STG_NO
        AND sa.RCRT_NO = ae.RCRT_NO
        AND sa.APL_NO = ae.APL_NO
        WHERE sa.DEPT_CD=#{deptNo} AND sa.RCRT_CD=#{rcrt} AND sa.EVL_STG_CD=#{10};
</select>

        List<Applicant> selectApplicantByOption(String year,String rcrt,String dept,String deptNo);


</mapper>
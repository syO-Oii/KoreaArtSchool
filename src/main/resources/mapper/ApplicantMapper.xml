<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.maximum.koreaartschool.dao.ApplicantMapper">

    <select id="selectEvaluatorApplicants" resultType="com.maximum.koreaartschool.dto.ApplicantEvaluate">
        SELECT *
        FROM DUMMY_APPLICANT_EVALUATE;
    </select>
<!--
    <select id="selectApplicantByDeptno" resultType="com.maximum.koreaartschool.dto.ApplicantEvaluate">
        SELECT *
        FROM DUMMY_APPLICANT_EVALUATE
        WHERE DEPT_CD = #{deptNo};
    </select>-->

</mapper>